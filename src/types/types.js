// Steps
// 1. Create the types folder.
// 2. Create the reducers folder.
// 3. Create the store folder.
// 4. Create the actions.
// 5. Connection of the provicer in the index.

export const types = {
    login: '[Auth] login',
    logout: 'logout',
    register: '[Auth] register'
}

export const typesItems ={
    register: 'register',
    list: 'list',
    delete: 'delete',
    active: 'active',
    search: 'search'

}